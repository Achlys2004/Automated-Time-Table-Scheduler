package com.university.scheduler.timetable_scheduler;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import com.university.scheduler.TimetableScheduleApplication;

@SpringBootTest(classes = TimetableScheduleApplication.class)
public class TimetableScheduleApplicationTests {

    @Test
    void contextLoads() {
    }
}